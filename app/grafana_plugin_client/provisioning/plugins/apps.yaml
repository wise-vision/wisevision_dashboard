apiVersion: 1

apps:
  - type: 'wisevision-wiseos-app'
    disabled: false
    # jsonData is intentionally empty - values are saved to database via UI
    # This prevents provisioning from overwriting user-configured values on restart
